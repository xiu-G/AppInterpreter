final class la.droid.qr.priva.bz extends android.view.GestureDetector$SimpleOnGestureListener
{
    private la.droid.qr.priva.Tabs a;

    void <init>(la.droid.qr.priva.Tabs)
    {
        la.droid.qr.priva.bz r0;
        la.droid.qr.priva.Tabs $r1;

        r0 := @this: la.droid.qr.priva.bz;

        $r1 := @parameter0: la.droid.qr.priva.Tabs;

        r0.<la.droid.qr.priva.bz: la.droid.qr.priva.Tabs a> = $r1;

        specialinvoke r0.<android.view.GestureDetector$SimpleOnGestureListener: void <init>()>();

        return;
    }

    public final boolean onFling(android.view.MotionEvent, android.view.MotionEvent, float, float)
    {
        la.droid.qr.priva.Tabs $r3;
        android.widget.TabHost $r4;
        java.lang.Exception $r6;
        la.droid.qr.priva.bz r0;
        byte $b1;
        android.view.MotionEvent $r1, $r2;
        int $i0, $i2;
        float $f0, $f1, $f2;
        android.widget.TabWidget $r5;
        boolean $z0;

        r0 := @this: la.droid.qr.priva.bz;

        $r1 := @parameter0: android.view.MotionEvent;

        $r2 := @parameter1: android.view.MotionEvent;

        $f0 := @parameter2: float;

        $f1 := @parameter3: float;

     label01:
        $f1 = virtualinvoke $r1.<android.view.MotionEvent: float getY()>();

        $f2 = virtualinvoke $r2.<android.view.MotionEvent: float getY()>();

     label02:
        $f1 = $f1 - $f2;

     label03:
        $f1 = staticinvoke <java.lang.Math: float abs(float)>($f1);

        $i0 = staticinvoke <la.droid.qr.priva.Tabs: int b()>();

     label04:
        $f2 = (float) $i0;

        $b1 = $f1 cmpl $f2;

        $i0 = (int) $b1;

        if $i0 <= 0 goto label05;

        return 0;

     label05:
        $r3 = r0.<la.droid.qr.priva.bz: la.droid.qr.priva.Tabs a>;

        $r4 = staticinvoke <la.droid.qr.priva.Tabs: android.widget.TabHost a(la.droid.qr.priva.Tabs)>($r3);

        $i0 = virtualinvoke $r4.<android.widget.TabHost: int getCurrentTab()>();

        $f1 = virtualinvoke $r1.<android.view.MotionEvent: float getX()>();

        $f2 = virtualinvoke $r2.<android.view.MotionEvent: float getX()>();

     label06:
        $f1 = $f1 - $f2;

     label07:
        $i2 = staticinvoke <la.droid.qr.priva.Tabs: int c()>();

     label08:
        $f2 = (float) $i2;

        $b1 = $f1 cmpl $f2;

        $i2 = (int) $b1;

        if $i2 <= 0 goto label16;

     label09:
        $f1 = staticinvoke <java.lang.Math: float abs(float)>($f0);

        $i2 = staticinvoke <la.droid.qr.priva.Tabs: int d()>();

     label10:
        $f2 = (float) $i2;

        $b1 = $f1 cmpl $f2;

        $i2 = (int) $b1;

        if $i2 <= 0 goto label16;

        $i0 = $i0 + -1;

        $z0 = 1;

     label11:
        if $z0 == 0 goto label24;

     label12:
        $r3 = r0.<la.droid.qr.priva.bz: la.droid.qr.priva.Tabs a>;

        $r4 = staticinvoke <la.droid.qr.priva.Tabs: android.widget.TabHost a(la.droid.qr.priva.Tabs)>($r3);

        $r5 = virtualinvoke $r4.<android.widget.TabHost: android.widget.TabWidget getTabWidget()>();

        $i2 = virtualinvoke $r5.<android.widget.TabWidget: int getTabCount()>();

     label13:
        if $i0 >= 0 goto label22;

        $i0 = $i2 - 1;

     label14:
        $r3 = r0.<la.droid.qr.priva.bz: la.droid.qr.priva.Tabs a>;

        $r4 = staticinvoke <la.droid.qr.priva.Tabs: android.widget.TabHost a(la.droid.qr.priva.Tabs)>($r3);

        virtualinvoke $r4.<android.widget.TabHost: void setCurrentTab(int)>($i0);

     label15:
        return 1;

     label16:
        $f1 = virtualinvoke $r2.<android.view.MotionEvent: float getX()>();

        $f2 = virtualinvoke $r1.<android.view.MotionEvent: float getX()>();

     label17:
        $f1 = $f1 - $f2;

     label18:
        $i2 = staticinvoke <la.droid.qr.priva.Tabs: int c()>();

     label19:
        $f2 = (float) $i2;

        $b1 = $f1 cmpl $f2;

        $i2 = (int) $b1;

        if $i2 <= 0 goto label25;

     label20:
        $f0 = staticinvoke <java.lang.Math: float abs(float)>($f0);

        $i2 = staticinvoke <la.droid.qr.priva.Tabs: int d()>();

     label21:
        $f1 = (float) $i2;

        $b1 = $f0 cmpl $f1;

        $i2 = (int) $b1;

        if $i2 <= 0 goto label25;

        $i0 = $i0 + 1;

        $z0 = 1;

        goto label11;

     label22:
        if $i0 < $i2 goto label14;

        $i0 = 0;

        goto label14;

     label23:
        $r6 := @caughtexception;

     label24:
        return 0;

     label25:
        $z0 = 0;

        goto label11;

        catch java.lang.Exception from label01 to label02 with label23;
        catch java.lang.Exception from label03 to label04 with label23;
        catch java.lang.Exception from label05 to label06 with label23;
        catch java.lang.Exception from label07 to label08 with label23;
        catch java.lang.Exception from label09 to label10 with label23;
        catch java.lang.Exception from label12 to label13 with label23;
        catch java.lang.Exception from label14 to label15 with label23;
        catch java.lang.Exception from label16 to label17 with label23;
        catch java.lang.Exception from label18 to label19 with label23;
        catch java.lang.Exception from label20 to label21 with label23;
    }
}
