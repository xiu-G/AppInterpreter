class oauth.signpost.signature.OAuthMessageSigner$1 extends java.lang.Object
{
    static final int[] $SwitchMap$oauth$signpost$signature$SignatureMethod;

    static void <clinit>()
    {
        java.lang.NoSuchFieldError $r3, $r4;
        int[] $r1;
        int $i0;
        oauth.signpost.signature.SignatureMethod[] $r0;
        oauth.signpost.signature.SignatureMethod $r2;

        $r0 = staticinvoke <oauth.signpost.signature.SignatureMethod: oauth.signpost.signature.SignatureMethod[] values()>();

        $i0 = lengthof $r0;

        $r1 = newarray (int)[$i0];

        <oauth.signpost.signature.OAuthMessageSigner$1: int[] $SwitchMap$oauth$signpost$signature$SignatureMethod> = $r1;

     label1:
        $r1 = <oauth.signpost.signature.OAuthMessageSigner$1: int[] $SwitchMap$oauth$signpost$signature$SignatureMethod>;

        $r2 = <oauth.signpost.signature.SignatureMethod: oauth.signpost.signature.SignatureMethod PLAINTEXT>;

        $i0 = virtualinvoke $r2.<oauth.signpost.signature.SignatureMethod: int ordinal()>();

     label2:
        $r1[$i0] = 1;

     label3:
        $r1 = <oauth.signpost.signature.OAuthMessageSigner$1: int[] $SwitchMap$oauth$signpost$signature$SignatureMethod>;

        $r2 = <oauth.signpost.signature.SignatureMethod: oauth.signpost.signature.SignatureMethod HMAC_SHA1>;

        $i0 = virtualinvoke $r2.<oauth.signpost.signature.SignatureMethod: int ordinal()>();

     label4:
        $r1[$i0] = 2;

        return;

     label5:
        $r3 := @caughtexception;

        return;

     label6:
        $r4 := @caughtexception;

        goto label3;

        catch java.lang.NoSuchFieldError from label1 to label2 with label6;
        catch java.lang.NoSuchFieldError from label3 to label4 with label5;
    }
}
