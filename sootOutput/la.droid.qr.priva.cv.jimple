final class la.droid.qr.priva.cv extends java.lang.Object implements android.content.DialogInterface$OnCancelListener
{
    private la.droid.qr.priva.Aplicaciones a;

    void <init>(la.droid.qr.priva.Aplicaciones)
    {
        la.droid.qr.priva.cv r0;
        la.droid.qr.priva.Aplicaciones $r1;

        r0 := @this: la.droid.qr.priva.cv;

        $r1 := @parameter0: la.droid.qr.priva.Aplicaciones;

        r0.<la.droid.qr.priva.cv: la.droid.qr.priva.Aplicaciones a> = $r1;

        specialinvoke r0.<java.lang.Object: void <init>()>();

        return;
    }

    public final void onCancel(android.content.DialogInterface)
    {
        java.lang.Throwable $r5;
        la.droid.qr.priva.cv r0;
        la.droid.qr.priva.cs $r2;
        java.lang.Exception $r4;
        android.content.DialogInterface $r1;
        la.droid.qr.priva.Aplicaciones $r3;

        r0 := @this: la.droid.qr.priva.cv;

        $r1 := @parameter0: android.content.DialogInterface;

     label1:
        $r3 = r0.<la.droid.qr.priva.cv: la.droid.qr.priva.Aplicaciones a>;

        $r2 = staticinvoke <la.droid.qr.priva.Aplicaciones: la.droid.qr.priva.cs e(la.droid.qr.priva.Aplicaciones)>($r3);

        virtualinvoke $r2.<la.droid.qr.priva.cs: boolean cancel(boolean)>(1);

     label2:
        $r3 = r0.<la.droid.qr.priva.cv: la.droid.qr.priva.Aplicaciones a>;

        virtualinvoke $r3.<la.droid.qr.priva.Aplicaciones: void finish()>();

        return;

     label3:
        $r4 := @caughtexception;

        $r3 = r0.<la.droid.qr.priva.cv: la.droid.qr.priva.Aplicaciones a>;

        virtualinvoke $r3.<la.droid.qr.priva.Aplicaciones: void finish()>();

        return;

     label4:
        $r5 := @caughtexception;

        $r3 = r0.<la.droid.qr.priva.cv: la.droid.qr.priva.Aplicaciones a>;

        virtualinvoke $r3.<la.droid.qr.priva.Aplicaciones: void finish()>();

        throw $r5;

        catch java.lang.Exception from label1 to label2 with label3;
        catch java.lang.Throwable from label1 to label2 with label4;
    }
}
