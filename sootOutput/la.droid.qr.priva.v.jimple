final class la.droid.qr.priva.v extends java.lang.Object implements android.content.DialogInterface$OnCancelListener
{
    private la.droid.qr.priva.Historial a;

    void <init>(la.droid.qr.priva.Historial)
    {
        la.droid.qr.priva.Historial $r1;
        la.droid.qr.priva.v r0;

        r0 := @this: la.droid.qr.priva.v;

        $r1 := @parameter0: la.droid.qr.priva.Historial;

        r0.<la.droid.qr.priva.v: la.droid.qr.priva.Historial a> = $r1;

        specialinvoke r0.<java.lang.Object: void <init>()>();

        return;
    }

    public final void onCancel(android.content.DialogInterface)
    {
        java.lang.Throwable $r5;
        la.droid.qr.priva.Historial $r3;
        la.droid.qr.priva.v r0;
        la.droid.qr.priva.bn $r2;
        java.lang.Exception $r4;
        android.content.DialogInterface $r1;

        r0 := @this: la.droid.qr.priva.v;

        $r1 := @parameter0: android.content.DialogInterface;

     label1:
        $r3 = r0.<la.droid.qr.priva.v: la.droid.qr.priva.Historial a>;

        $r2 = staticinvoke <la.droid.qr.priva.Historial: la.droid.qr.priva.bn f(la.droid.qr.priva.Historial)>($r3);

        virtualinvoke $r2.<la.droid.qr.priva.bn: boolean cancel(boolean)>(1);

     label2:
        $r3 = r0.<la.droid.qr.priva.v: la.droid.qr.priva.Historial a>;

        virtualinvoke $r3.<la.droid.qr.priva.Historial: void finish()>();

        return;

     label3:
        $r4 := @caughtexception;

        $r3 = r0.<la.droid.qr.priva.v: la.droid.qr.priva.Historial a>;

        virtualinvoke $r3.<la.droid.qr.priva.Historial: void finish()>();

        return;

     label4:
        $r5 := @caughtexception;

        $r3 = r0.<la.droid.qr.priva.v: la.droid.qr.priva.Historial a>;

        virtualinvoke $r3.<la.droid.qr.priva.Historial: void finish()>();

        throw $r5;

        catch java.lang.Exception from label1 to label2 with label3;
        catch java.lang.Throwable from label1 to label2 with label4;
    }
}
